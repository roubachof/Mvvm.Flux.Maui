<?xml version="1.0" encoding="UTF-8" ?>
<ContentView x:Class="Symbiot.Mobile.Presentation.CustomViews.Toolbar"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:Symbiot.Mobile.Presentation.Converters;assembly=Symbiot.Mobile"
             xmlns:sho="http://sharpnado.com"
             xmlns:xaml="http://prismlibrary.com"
             x:Name="RootLayout">
    <ContentView.Resources>
        <ResourceDictionary>
            <Style x:Key="TextTitleStyle"
                   BasedOn="{StaticResource TextTitleToolbar}"
                   TargetType="Label">
                <Setter Property="HorizontalOptions">
                    <Setter.Value>
                        <OnPlatform x:TypeArguments="LayoutOptions">
                            <On Platform="iOS">Start</On>
                            <On Platform="Android">Start</On>
                        </OnPlatform>
                    </Setter.Value>
                </Setter>
                <Setter Property="Margin" Value="30,10" />
                <Setter Property="VerticalOptions" Value="Center" />
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>
    <ContentView.Content>
        <Grid x:Name="Grid"
              ColumnSpacing="0"
              RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="SafeAreaRowDefinition" Height="{Binding Source={x:Reference RootLayout}, Path=Height, Converter={converters:TopSafeAreaToGridLengthConverter}, ConverterParameter=0}" />
                <RowDefinition Height="*" />
                <RowDefinition x:Name="SubtitleRowDefinition" Height="0" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ButtonColumnDefinition" Width="0" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <BoxView Grid.Row="0"
                     Grid.ColumnSpan="3"
                     BackgroundColor="{Binding Source={x:Reference RootLayout}, Path=BackgroundColor}" />

            <ContentView x:Name="BackButton"
                         Grid.Row="1"
                         Grid.Column="0"
                         sho:TapCommandEffect.Tap="{xaml:GoBack}"
                         sho:ViewEffect.TouchFeedbackColor="{StaticResource PrimaryColor}"
                         IsVisible="{Binding Source={x:Reference RootLayout}, Path=ShowBackButton}">
                <Image Style="{StaticResource ImageSmallLarge}"
                       sho:ImageEffect.TintColor="{Binding Source={x:Reference RootLayout}, Path=ForegroundColor}"
                       Source="{StaticResource IconBack}" />
            </ContentView>

            <Label x:Name="TitleLabel"
                   Grid.Row="1"
                   Grid.Column="1"
                   Style="{StaticResource TextTitleStyle}"
                   Text="{Binding Source={x:Reference RootLayout}, Path=Title}"
                   TextColor="{Binding Source={x:Reference RootLayout}, Path=ForegroundColor}" />

            <Label Grid.Row="2"
                   Grid.Column="1"
                   Style="{StaticResource TextBody}"
                   Margin="30,0,30,30"
                   Text="{Binding Source={x:Reference RootLayout}, Path=Subtitle}"
                   TextColor="{Binding Source={x:Reference RootLayout}, Path=ForegroundColor}" />

            <ContentView x:Name="CustomViewRoot"
                         Grid.Row="1"
                         Grid.RowSpan="2"
                         Grid.Column="2" />
        </Grid>
    </ContentView.Content>
</ContentView>